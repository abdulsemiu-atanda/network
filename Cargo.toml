[package]
name = "network"
authors = ["Abdulsemiu Atanda <semiu@live.com>"]
version = "0.1.0"
edition = "2024"
rust-version = "1.88"

[lib]
name="network"
path="src/lib.rs"

[dependencies]
diesel = { version = "2.3.7", optional = true, features = ["extras", "postgres"] }
log = "0.4.29"
redis = { version = "1.2.0", optional = true }
reqwest = { version = "0.13.2", features = ["json"], optional = true }
serde = "1.0.228"
serde_derive = "1.0.228"

[dev-dependencies]
tokio = { version = "1.51.1", features = ["macros", "rt-multi-thread"] }
httpmock = "0.8.3"
serde_json = "1"

[features]
default = ["http"]
database = ["diesel"]
keystore = ["redis"]
http = ["reqwest"]
spec = ["database"]
